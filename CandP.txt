 <!-- <title></title>
</head>
<body>
    <header>
        <nav class="nav">
            <ul class="nav nav-tabs">
                <li class="nav-item">
                {% if page == "home" %}
                    <a class="nav-link active" aria-current="page" href="#">
                {% else %}
                    <a class="nav-link" href="{{ url_for('home') }}">
                {% endif %}
                      Home</a>
                </li>
                <li class="nav-item">
                {% if page == "add_goal" %}
                    <a class="nav-link active" aria-current="page" href="#">
                {% else %}
                    <a class="nav-link" href="{{ url_for('add_goal') }}">
                {% endif %}
                  Add Goal</a>
                </li>
                <li class="nav-item">
                {% if page == "complete_goals" %}
                    <a class="nav-link active" aria-current="page" href="#">
                {% else %}
                    <a class="nav-link" href="{{ url_for('complete_goals') }}">
                {% endif %}
                    Complete Goals</a>
                </li>
                <li class="nav-item">
                    {% if page == "logout" %}
                        <a class="nav-link active" aria-current="page" href="#">
                    {% else %}
                        <a class="nav-link" href="{{ url_for('login') }}">
                    {% endif %}
                        Log Out</a>
                    </li>
              </ul>
            </nav>
    </header> -->